function tellFortune(numChildren, mateName, geoLocal, jobTitle) {
  
    

console.log('I am a ' + jobTitle + ' in ' + geoLocal + ' and I am married to ' + mateName + ' with ' + numChildren + ' kids.')
}

tellFortune(3, 'Step', 'Atlanta', 'Program Designer');
tellFortune(10, 'Andrea', 'Athens', 'Financial Planner');
tellFortune(4, 'Mallory', 'San Deigo', 'Microvascular Surgeon');


/*function calculateSupply(age, dailyAmount ) {

    
        const maxAge = 113
        lifeAmount = parseFloat(maxAge - age) * (dailyAmount * 365)
        let result = Math.round(lifeAmount) 
        console.log('You will need ' + lifeAmount + ' of RapSnacks to last you until the ripe old age of ' + maxAge )
}

calculateSupply(41, 2.5);

function calcCircumference(radius, ) {
       
     circumference = (2 * Math.PI * radius)
    console.log('The circumference is ' + circumference)
}

function calArea(radius,) {
    
    area =  Math.PI * radius * radius
    console.log('the area is ' + area)
}
calcCircumference(14);
calArea(26);*/